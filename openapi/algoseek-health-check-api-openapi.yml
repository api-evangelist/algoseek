openapi: 3.2.0
info:
  title: metadata-api Healthcheck API
  version: '1.2'
tags:
- name: Health Check
paths:
  /api/v1/healthcheck/:
    get:
      summary: Healthcheck
      operationId: healthcheck_api_v1_healthcheck__get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      tags:
      - Health Check
components:
  securitySchemes:
    HTTPBearer:
      type: http
      scheme: bearer
